import { ProcessingPipe } from './processing.pipe';

describe('ProcessingPipe', () => {
  it('create an instance', () => {
    const pipe = new ProcessingPipe();
    expect(pipe).toBeTruthy();
  });
});
